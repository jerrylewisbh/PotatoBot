#!/usr/bin/bash

#autoflake -r --exclude utils tests --remove-unused-variables --remove-all-unused-imports --in-place ${dir##*/}
#autopep8 ${dir##*/} -r --in-place --experimental --aggressive --max-line-length 120
#isort -rc ${dir##*/}
